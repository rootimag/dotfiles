#!/usr/bin/env bash

# Define the path to the history file
HISTORY_FILE="$HOME/.cache/tgpt_history.txt"

# Check the parameters
if [[ -n "$1" ]]; then
    tgpt "$1" --log "$HISTORY_FILE"
else
    tgpt -i --log "$HISTORY_FILE"
fi
